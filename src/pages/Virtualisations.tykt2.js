/**
 * Virtualisations — /cloud-computing/virtualisations
 * SEO: src/seo/pages.json → virtualisations
 */

$w.onReady(function () {
  if ($w('#pageTitle')) {
    $w('#pageTitle').text = 'Virtualisation services';
  }
  if ($w('#pageBody')) {
    $w('#pageBody').text =
      'Consolidate and modernise workloads with proven virtualisation patterns aligned to your cloud roadmap.';
  }
});
